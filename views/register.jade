extends ./layoutT.jade
block contenido
	div.col-md-6.remove-float.center-block.big-top-space
		form(action="/users" method="POST")
			div.form-group
				label(for="email") Email
				input.form-control#email(type="text", name="email", placeholder="Email")
			div(class="form-group")
				label(for="username") Username
				input.form-control#username(type="text", name="username", placeholder="Nombre de usuario")
			div(class="form-group")
				label(for="password") Contraseña
				input.form-control#password(type="password", name="password", placeholder="Contraseña")
			div(class="form-group")
				label(for="password_confirmation") Confirma tu contraseña
				input.form-control#password_confirmation(type="password", name="password_confirmation", placeholder="Confirma tu contraseña")
			div.row.top-space
				div.col-xs-12.col-sm-6
					input.btn.btn-info(type="submit", value="Iniciar session")
				div.col-xs-12.col-sm-6.text-right
					a(href="/login") Ya tengo una cuenta